spring:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/colis-db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=convertToNull
    username: root
    password: MerciBaba_10
    testWhileIdle: true
    validationQuery: SELECT 1

  jpa:
    database: default
    properties:
      hibernate:
        ddl-auto: update
        show_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
      naming.physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  profiles:
     active: postgres

logging:
  level:
    root: DEBUG
    org.springframework: DEBUG
    org.hibernate: ERROR